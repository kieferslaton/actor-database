{
  "dependencies": {
    "bcrypt": "^5.0.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^5.9.26",
    "nodemon": "^2.0.4",
    "path": "^0.12.7"
  }, 
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm i --only=dev && npm i && npm run build"
  }
}
